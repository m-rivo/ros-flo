---
// Define tu arreglo de preguntas frecuentes aquí
const faqs = [
  {
    id: 'one', // ID único para cada pregunta
    question: '¿Cómo puedo agendar una cita?',
    answer: 'Puedes agendar una cita directamente desde nuestro calendario de Google en la sección de contacto o llamando a nuestro número.',
    active: true // Establece si esta pregunta debe estar abierta por defecto
  },
  {
    id: 'two',
    question: '¿Cuál es su horario de atención?',
    answer: 'Nuestro horario de atención es de lunes a viernes, de 8:00 AM a 5:00 PM, y los sábados de 9:00 AM a 12:00 PM.',
    active: false
  },
  {
    id: 'three',
    question: '¿Dónde están ubicados?',
    answer: 'Estamos ubicados en Tegucigalpa, Francisco Morazán, Honduras. Puedes ver nuestra dirección exacta en el mapa de la sección de contacto.',
    active: false
  },
  {
    id: 'four',
    question: '¿Aceptan seguros dentales?',
    answer: 'Sí, aceptamos los principales seguros dentales. Te recomendamos contactarnos para verificar si tu seguro específico es compatible.',
    active: false
  }
];
---

<div class="max-w-[85rem] px-4 py-10 sm:px-6 lg:px-8 lg:py-14 mx-auto">
  <div class="max-w-2xl mx-auto text-center mb-10 lg:mb-14">
    <h2 class="text-2xl font-bold md:text-4xl md:leading-tight text-primary">
      Tus preguntas, respondidas
    </h2>
    <p class="mt-1 text-gray-600">Respuestas a las preguntas más frecuentes.</p>
  </div>
  <div class="max-w-2xl mx-auto">
    <div
      id="accordion-flush"
      data-accordion="collapse"
      data-active-classes="bg-white text-gray-900"
      data-inactive-classes="text-gray-500"
    >
      {faqs.map((faq, index) => (
        <h2 id={`accordion-flush-heading-${faq.id}`}>
          <button
            type="button"
            class={`flex items-center justify-between w-full py-5 font-medium rtl:text-right border-b border-gray-200 gap-3
                    ${faq.active ? 'text-gray-900' : 'text-gray-500'}
                    ${index === 0 && faq.active ? '' : ''}
                    `}
            data-accordion-target={`#accordion-flush-body-${faq.id}`}
            aria-expanded={faq.active ? 'true' : 'false'}
            aria-controls={`accordion-flush-body-${faq.id}`}
          >
            <span>{faq.question}</span>
            <svg
              data-accordion-icon
              class={`w-3 h-3 shrink-0 ${faq.active ? 'rotate-180' : ''}`}
              aria-hidden="true"
              xmlns="http://www.w3.org/2000/svg"
              fill="none"
              viewBox="0 0 10 6"
            >
              <path stroke="currentColor" stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5 5 1 1 5"/>
            </svg>
          </button>
        </h2>
        <div
          id={`accordion-flush-body-${faq.id}`}
          class={`${faq.active ? '' : 'hidden'}`}
          aria-labelledby={`accordion-flush-heading-${faq.id}`}
        >
          <div class="py-5 border-b border-gray-200">
            <p class="mb-2 text-gray-500">
              {faq.answer}
            </p>
          </div>
        </div>
      ))}
    </div>
    </div>
</div>